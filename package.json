{
  "name": "cronpulse",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute cronpulse-prod --local --file=src/db/schema.sql",
    "db:init:remote": "wrangler d1 execute cronpulse-prod --remote --file=src/db/schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.11.9",
    "nanoid": "^5.1.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260210.0",
    "wrangler": "^4.64.0",
    "typescript": "^5.9.3"
  }
}
